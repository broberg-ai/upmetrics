export function Probes() {
  return (
    <div>
      <h1 class="text-xl font-semibold">Probes</h1>
      <p class="mt-1 text-sm text-[var(--muted)]">Uptime probes grid + history — lands in F006.5.</p>
    </div>
  );
}
