export function Agents() {
  return (
    <div>
      <h1 class="text-xl font-semibold">Agents</h1>
      <p class="mt-1 text-sm text-[var(--muted)]">AI agent telemetry — cost, sessions, tool failures — lands in F006.4.</p>
    </div>
  );
}
