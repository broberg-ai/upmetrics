export function Issues() {
  return (
    <div>
      <h1 class="text-xl font-semibold">Issues</h1>
      <p class="mt-1 text-sm text-[var(--muted)]">Error groups — list, detail + stack trace — lands in F006.3.</p>
    </div>
  );
}
