// Open-incidents bar shown on every page (F015). Wired to live data in F006.5;
// for now it renders nothing until the incidents read endpoint lands.
export function IncidentsBar() {
  return null;
}
