export function Overview() {
  return (
    <div>
      <h1 class="text-xl font-semibold">Overview</h1>
      <p class="mt-1 text-sm text-[var(--muted)]">Per-project health + global matrix — lands in F006.2.</p>
    </div>
  );
}
